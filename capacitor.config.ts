import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-radio-small',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
